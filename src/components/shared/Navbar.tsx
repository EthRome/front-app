import { NAVBAR_HEIGHT } from '../../utils/constants';

export const Navbar = () => {
  return (
    <div style={{ height: `${NAVBAR_HEIGHT}px` }} className='border-b-2 w-full p-4'>
      Navbar
    </div>
  );
};
