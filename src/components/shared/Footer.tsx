import { FOOTER_HEIGHT } from '../../utils/constants';

export const Footer = () => {
  return (
    <div style={{ height: `${FOOTER_HEIGHT}px` }} className='border-t-2 w-full p-4'>
      Footer
    </div>
  );
};
